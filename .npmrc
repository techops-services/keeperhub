# Force-hoist @workflow/world-postgres transitive deps to top-level node_modules.
# Next.js standalone output tracing (nft) cannot follow the dynamic
# require(process.env.WORKFLOW_TARGET_WORLD) in the workflow SDK, so we
# manually include these packages via outputFileTracingIncludes in next.config.ts.
# pnpm's default strict mode keeps transitive deps in .pnpm/ which the
# simple ./node_modules/<pkg> globs can't reach — hoisting solves this.
public-hoist-pattern[]=pg-boss
public-hoist-pattern[]=@workflow/*
public-hoist-pattern[]=@vercel/queue
public-hoist-pattern[]=@vercel/oidc
public-hoist-pattern[]=mixpart
public-hoist-pattern[]=cbor-x
public-hoist-pattern[]=cbor-extract
public-hoist-pattern[]=ulid
public-hoist-pattern[]=async-sema
public-hoist-pattern[]=cron-parser
